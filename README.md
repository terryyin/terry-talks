terry-talks
===========

Terry's talks and presentations, built with [Slidev](https://sli.dev/).
Each talk lives under `slides/<deck>/slides.md`; run `pnpm run show` to pick
one and launch it (`show` is a reserved pnpm command, so the `run` is
required). Retired notebook-era content lives under `legacy/`.
